name: Update Garden Data

on:
  # Run every 5 minutes
  schedule:
    - cron: '*/5 * * * *'
  
  # Allow manual trigger
  workflow_dispatch:

  # Also run on push to main (for testing)
  push:
    branches:
      - main
    paths:
      - 'collect-data.ts'
      - 'schema.ts'
      - '.github/workflows/update-data.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Check for new or removed apps
        run: npx tsx collect-data.ts --check-apps

      - name: Collect fresh data
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx tsx collect-data.ts
        timeout-minutes: 10

      - name: Copy data to website
        run: cp apps.json website/src/data.json

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet apps.json website/src/data.json || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push changes
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add apps.json website/src/data.json
          git commit -m "chore: update garden data $(date -u +'%Y-%m-%d %H:%M UTC')"
          git push
